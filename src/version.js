// Single build stamp for the app shell. Must match VERSION in ../sw.js.
// Static rewrite (Plan/webapp-benchmark C-1) — same features, no build step.
export const APP_BUILD = '2026.09.05-freshstart1'
